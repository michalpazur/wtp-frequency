import { SxProps } from "@mui/material";

export const lineStyle: SxProps = {
  fontSize: "1rem",
  textAlign: "center",
};

export const headwaysContainer: SxProps = {
  display: "flex",
  flexWrap: "wrap",
};
